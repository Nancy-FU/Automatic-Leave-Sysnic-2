# VSYS 请假自动化 — 变更日志 (CHANGELOG)

> **本文件记录：规则是怎么演变成今天这样的 + 近期操作 + 还欠什么。**
> 不重复规则正文——现行规则以 `PRD.md` / `PHASE2_DESIGN.md` / `roster.json` 为准，冲突时以它们为准。
>
> **历史明细已归档**（未随本仓库公开同步，仅私有仓库保留）：
> - 2026-07 ~ 08 Phase 1 建设期与历史数据全面清理（原 A–AG 段，逐条操作明细）→ `archive/CHANGELOG_2026-07_phase1-buildout.md`
> - 更早的 Codex / Apps Script 三次尝试 → `archive/OLD_VSYS_LEAVE_SYNC_PRD_AND_CHANGELOG.md`

## 版本状态

- ✅ **Phase 1 + 1b — 已上线稳定**（git `main` 基线，tag `phase-1` / `phase-1b-merged`）
  邮件 → 日历自动同步：全假别（AL/SL/BL）→ **VSYS Leaves**；WFH / OOO → **VSYS MY Attendance**；混合两边各记一条。历史数据已修正 + 姓名已标准化 + HK/Malaysia office 分类。定时任务 `vsys-leave-sync` 工作日 9:00 / 15:00 运行。
- 🚧 **Phase 2a — 闭环已跑通**（git `phase-2` 分支，`main` 可随时回撤）
  「月度只读比对 → 用户在场同步表格」于 **2026-09-03 首次端到端跑通**（8 月份 8 行 / 4 个 tab，见下方 AH/AI 段）。定时任务 `vsys-phase2-monthly-diff` 每月第一个星期一只读运行。
- 📋 **Phase 2b — 未开始**：季度提醒邮件。

---

## 规则演变时间线（2026-07 ~ 08）

逐条操作明细见私有仓库的 `archive/CHANGELOG_2026-07_phase1-buildout.md`（段落字母对应该文件）。

| 日期 | 段 | 发生了什么 | 沉淀成了什么 |
|---|---|---|---|
| 07-14 | A–B | 诊断出旧「自动化」是多次零散尝试；Apps Script 20 个触发器仍在跑且错误率 100% | 退役全部旧自动化，撤销云端授权从源头封死 |
| 07-14 | C–D | 一次性修正历史日历：累计新建 ~34、修改 ~17、删除 5 | 年份错 / 审批人误记 / 重复 / 假别错四类问题清零 |
| 07-14 | I–K | 建立姓名标准化：扫 183 条事件，改名 136、删重复 6 | **`roster.json`** 成为姓名 + office + 路由的唯一来源 |
| 07-14 | M | Employee 01 7/27–8/3 首个端到端真实用例通过 | 解析 → 命名 → 写入链路验证 |
| 07-15 | N | 搭建定时任务 `vsys-leave-sync`（工作日 9:00/15:00） | Phase 1 无人值守上线 |
| 07-15 | O–P | Phase 1b：扩到全假别 + WFH + 混合 + office 分类 | `roster.json` v4：`calendarRouting` / `officeClassification` |
| 07-16 | **R–S** | **发现系统性污染：旧自动化把审批人误记成请假人**（经理为重灾区） | **「邮件为准」防错机制** → PRD §5 |
| 07-16~17 | Q,T–X | 全日历清扫 + 6 人老员工表格按邮件重建 + 表格结构升级（新增 Birthday 列、修 Others 公式、加 BL 下拉） | 8 个 tab 结构统一 |
| 07-17 | **Y** | Nancy tab 误「清空重做」，被用户要求回撤 | **「先核对完整性，再针对性修」** → 长期记忆 |
| 07-21 | **Z** | 用户发现日历描述栏泄露请假原因和审批人原话 | **描述栏只留 4 项**（Source email / From / Message date / Approved by）→ PRD §3 |
| 07-21 | AA | 回溯清理 6–7 月：改写 10 条、清空 4 条 | 历史泄露已清 |
| 07-21 | **AB** | 讨论后选定**方案 B**：日历实时自动写；表格改为「定时只读比对 + 用户在场手动同步」 | 理由：Sheets 无写 API，只能浏览器模拟，无人值守写错没人当场发现 |

---

## 沉淀下来的教训（规则背后的「为什么」）

1. **邮件 = 最终确认版本，日历只做 cross-check。** 登记前必须找到「此人本人」发出的原始请假邮件；仅在审批/回复里出现 → 不记。根因见归档 R 段（审批人误记是系统性污染，不是偶发）。
2. **已有数据先核对完整性，不要清空重做。** 归档 Y 段的教训，已入长期记忆。
3. **浏览器填表绝不跨列用 Tab。** Tab 在 Sheets 里不跳格，会把 From/To 挤进同一格（Employee 01 / Nancy 两次事故）。改用名称框跳转定位每列起点，见 PHASE2_DESIGN §4.1。
4. **改了规则，要同步改「正在跑的那个东西」。** 7/21 定的隐私格式只改了 PRD，忘了改 `vsys-leave-sync` 的 prompt，直到 8/4 才发现它还在按旧格式写入（归档 AE 段）。**改 PRD 后必须检查 `~/.claude/scheduled-tasks/*/SKILL.md`。**
5. **日历假别粒度 ≠ 表格假别粒度。** 日历只有 `AL:/SL:/BL:` 三种前缀，Comp Leave / Birthday Leave 在日历上一律归入 `AL:`，属设计如此——**不是冲突，别标异常、别改日历**（2026-09-03 曾误报一次，见 AH 段）。

---

## 2026-08-04 — VSYS MY WFH 改名为 VSYS MY Attendance + 新增 OOO 类型

（原 AC–AG 段，规则正文见 `PRD.md` §3.3 与 `roster.json` v5 `oooRule`）

- **起因**：Employee 10 的 "Out Of Office" 自动回复邮件该记在哪。核实确认它不是正式请假申请（无假别、无起始日、无审批），不该记成 AL/SL。
- **用户决定**：OOO 与 WFH 一起进原 WFH 日历，该日历已改名 **"VSYS MY Attendance"**（ID 不变）。
- **OOO 规则**：命名 `OOO: <PreferredName>`；**判定门槛低于正式请假**（自动回复是本人邮箱发的，无「审批人误记」风险，不需要找批准邮件）；**起始日用该邮件自身日期推算**并在描述里注明；**不进 Phase 2 表格**，不扣任何余额。
- 已接入 `vsys-leave-sync`（新增 Step 3b）；已建 `OOO: Employee 10` 2026-07-30 → 08-09。
- 顺带修复了教训 4 里那个「隐私格式没同步到定时任务」的漏洞。

---

## 2026-09-03 — Phase 2 首次月度比对（8 月）+ 首次表格同步 8 行 + 三类修补常设授权

### AH. `vsys-phase2-monthly-diff` 首跑（覆盖 2026-08-01 ~ 08-31）

- 第一个星期一自检通过（9/3，日期 3 ≤ 7）。**8 月无香港公众假期**，当月工作日 = 全部平日。
- 邮件确认 8 条待登记（限 8 人 tab 范围）：Employee 03 2、Employee 04 2、Nancy 3、Employee 06 1。**Employee 01 / Employee 05 / Employee 02 / Employee 07 当月无任何请假或 WFH 邮件**。范围外人员仅作脚注，不列 to-do。
- **跨月边界发现（已规则化）**：Employee 04 8/26 发的邮件申请的是 **9/3 WFH + 9/4 AL**。下月比对只搜「9 月发出的邮件」，这封 8 月邮件届时搜不到 → 不当场标出就永久漏登记。凡**邮件日期在窗口内、请假日期落在下月**的，一律单列提醒，不静默丢弃。（已写入 PRD §5 规则 10、PHASE2_DESIGN §3、任务 prompt）
- **一处误报已撤回**：初稿把 Nancy 8/27、8/28 的 `AL: Nancy` 标成「日历与邮件不符」，复核后确认是设计如此（见教训 5），日历无需修改。

### AI. 首次表格同步（用户在场，说 "sync sheet" 后执行）

8 行全部按 §4.1 逐列填法写入（绝不跨列用 Tab），写完用 Drive 连接器**独立回读**核对：

| tab | 新增明细 | Summary 变化 |
|---|---|---|
| Employee 03 | 5-Aug AL 1；7-Aug→10-Aug AL 2 | AL 6→9，Balance 8.0→**5.0** |
| Employee 04 | 5-Aug AL 1；7-Aug→10-Aug AL 2 | AL 6→9，Balance 14.0→**11.0** |
| Nancy | 26-Aug WFH 1；27-Aug Comp Leave 1；28-Aug Birthday Leave 1 | Comp 0→1、WFH 3→4、Birthday 0→1，AL Balance **9.30 不变**（三条均非 AL） |
| Employee 06 | 6-Aug→7-Aug AL 2 | AL 0→2，Balance 19.30→**17.30** |

- 8/7（五）→8/10（一）跨周末按 §3.4 只算 2 个工作日；Employee 03 与 Employee 04 同期请假是两人各自的邮件，非重复记录。
- Nancy 生日假校验通过（生日 8/29，8/28 在生日当月且仅 1 天）。
- Employee 04 9/3–9/4 按用户指示暂不写入，留到 9 月比对时登记。

### AJ. 用户新授权：以下三类修补直接做，不再逐次询问（2026-09-03 确认）

1. **日期显示格式不一致** → 直接统一为该 tab From 列的 `d-Mmm-yyyy` 样式。*本次修 Employee 06 tab 的 To 列（B10:B20），底层日期值本来就正确。*
2. **下拉验证范围没盖住新行** → 直接用「选择性粘贴 → 仅数据验证」向下延伸。*本次修 Nancy tab C22 的 "Birthday Leave"，值和 SUMIF 本来就对，只是没下拉框。*
3. **历史 From/To 挤在同一格的旧 Tab 键 bug** → 直接拆开修正。*本次修 Employee 04 tab 第 12 行，Summary 不受影响。*

> 三条已写入 `PRD.md` §5 规则 9 与 `PHASE2_DESIGN.md` §4.4，作为常规维护动作执行。

---

## 待办（下一步）

### 需要动手的

1. **Employee 04 9/3 WFH + 9/4 AL** ⚠️ — 8/26 邮件已确认（审批人已批准），当时未写表格。**9 月比对时务必登记**；该邮件是 8 月发出的，9 月窗口搜索不会再命中它。
2. **Nancy tab** — 按「先核对完整性、再针对性修」处理。归档 Y 段列了待核对点：5-Dec 应为 AM 半天（非 PM）；4 天假应 30-Mar 起（非 31-Mar，公式 C12 漏算 5-Dec / 2-Jan 使总额显示 8 而非 8.5）；缺 3-Mar、29-May 两天 WFH；20-Apr 病假半/整天待定。
3. **Phase 2b** — 季度提醒邮件（每次发送需用户确认）。
4. （可选）统一历史 WFH 日历里残留的旧姓名写法到 Preferred Name。

### 需要用户决定 / 用户自行处理的

5. **确认 SL 是否也要从 VSYS Leaves 挪到 VSYS MY Attendance**（当前未改）。Attendance 日历的描述里写了含 SL，但用户当初只确认了 WFH + OOO。影响面较大，需明确指示。
6. **手动删掉 "VSYS MY Attendance " 日历名尾部的空格** — 连接器只能读写事件，改不了日历本身的 summary，需用户在 Google Calendar 设置里改。
7. Employee 01 以外 6 个老员工 tab 的 From/To 分列（用户表示自行清理）。
8. 老员工表格 go-through 复核（AL 余额见归档 V 段）。

### 历史遗留待人工确认（低优先，来自已归档段落）

9. `AL: Employee 30` 2026-05-28 有两条重叠（手动 2 天 5/28–29 + 邮件来源 1 天 5/28），按「不合并不同时段」规则未动。（归档 L 段）
10. Employee 18 2025-12-22~24：邮件「半天」与「3 天」矛盾，当前记为 3 天全天。（归档 D 段）
11. Employee 25 4/10：按邮件原文 "On leave" 记为整天，原预期半天。（归档 D 段）

### 已完成（勾除）

- ~~`vsys-phase2-monthly-diff` 首次运行 + 首次现场同步表格~~ ✅ 2026-09-03（AH/AI 段），以后每月照此流程
- ~~执行姓名标准化改名~~ ✅ 2026-07-14（归档 J/K 段）
- ~~Employee 01 7/27–8/3 测试用例写入~~ ✅ 2026-07-14（归档 M 段）
