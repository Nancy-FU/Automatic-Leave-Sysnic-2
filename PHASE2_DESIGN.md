# Phase 2 设计文档 — 年假记录表自动登记

**分支:** `phase-2`
**状态:** 设计定稿，尚未实施（未碰表格）
**创建:** 2026-07-15

---

## 1. 目标与范围

每次收到请假/WFH 邮件，在更新日历（Phase 1/1b 已做）的**同时**，把该请假登记进 **Leave Record QB 2026** 表格，并维护每人的已用/结余。

- **Phase 2a（本文档）**：表格自动登记 + 结余维护。
- **Phase 2b（以后再设计）**：每季度给每人发邮件提醒剩余年假。发邮件属于"代发消息"，每次需你确认，留待 2b。

**范围**：先只维护现有 **8 个 tab**：Employee 03、Employee 04(Wong)、Employee 01、Employee 05、Employee 02、Employee 06、Nancy、Employee 07。

---

## 2. 目标文件

- 名称：**Leave Record QB 2026**
- fileId：`<LEAVE_RECORD_SHEET_FILE_ID>`
- 位置：My Drive / Leave Record / Leave Record_QB /
- 一人一个 tab。

### 每个 tab 的结构（现状）

| 区块 | 内容 |
|---|---|
| 抬头行 | Staff Name / Company Name / Position |
| 参数行 | Joined Date / No. of Annual Leave（年假总额）/ Entitled AL as of 31 Dec 2026（本年可用额，含结转/折算） |
| Summary 表头 | `Summarry \| AL \| SL \| Comp Leave \| WFH \| Others` |
| Summary 行1 | Total days of leave taken（各类已用天数） |
| Summary 行2 | Balance（AL 结余 = Entitled AL − AL 已用） |
| 明细表头 | `From \| To \| Type of Leave \| No. of day(s) Taken \| Remarks` |
| 明细行 | 每条请假一行 |

> **结构改动（Phase 2a 首次实施时做一次）**：在 Summary 里为 **Birthday Leave** 增加独立一列（目前它混在 Others 里）。改动顺序：Summary 表头加 "Birthday Leave" 列 + 对应的 Total/公式。

---

## 3. 计算规则（定稿）

### 3.1 年假额度 Entitled AL
- `No. of Annual Leave` = 每人固定年假总额（如 15/18/19/20）。
- **入职当年（仅新人）折算**：`Entitled = 入职日→当年 12/31 的日历天数 ÷ 365 × 年假总额`。（例：Employee 07 2025-06-22 入职，15 × 192/365 = 7.89。此规则只在入职那一年出现。）
- **入职后的完整年份**：`Entitled = 年假总额 + 上年结转（≤5）`。

### 3.2 结转 / 结清（Carry-over；无买断）
- 每年 **12/31 结清**：未用 AL **最多结转 5 天**到次年，超过 5 天的部分**销掉**。
- **公司无 cash out（买断）逻辑**，只有结转。
- **实现（Option A + C）**：
  - **A（公式）**：次年 tab 的 Entitled AL 单元格用公式自动算结转，例如 `= 年假总额 + MIN(上年 AL Balance, 5)`。数学交给表格，透明可查。
  - **C（人工复核）**：年底由 Claude 提醒 + 协助建次年 tab（引用上年 balance），你确认后生效。不做全自动（年底一次错影响全员，风险高）。

### 3.3 假别与扣减
- **只有 AL 扣减 AL 结余**。
- **SL / Comp Leave / WFH / Birthday Leave / Others / BL 全部单独计，不扣 AL**，各自累加到 Summary 对应列。
- **Summary 列（2026-07-17 升级后）**：AL / SL / Comp Leave / WFH / Others / **Birthday Leave**（新列 G）。
  - 各列合计为对明细 Type 列的 SUMIF。**"Others" 列用通配** `SUMIF(C,"Others*",D)+SUMIF(C,"BL",D)`（否则精确匹配 "Others" 会漏掉 "Others, refer to remarks"）。
  - **Balance = Entitled − AL 已用**，公式 `=F3−B8`（Employee 06/Nancy/Employee 07 布局为 `=B?−...`，按 tab 调整；显示 1 位小数）。
- **类型下拉选项**（8 tab 统一）：AL / SL / Birthday Leave / WFH / Comp Leave / Others, refer to remarks / **BL**。
- **BL（丧假）**：有下拉选项，但 **Summary 折入 Others 列**（用户 2026-07-17 定，不单独设列）。
- **Birthday Leave**：
  - Summary **单独一列**（G 列）；明细 Type 写 "Birthday Leave"。
  - **限制**：只能在本人**生日当月**用、**最多 1 天**。违反 → 标红 + 报告提示。
  - 生日数据已入 `roster.json`（`birthday` 字段，7/8；Employee 06 忽略）。

### 3.4 天数算法（No. of day(s) Taken）—— 关键规则
- **扣假期天数一律只算工作日**：**必须跳过周末（周六、周日）和法定节假日（香港公众假期）**。落在周末/公众假期上的日期不计入天数。
- 半天 = **0.5**。
- **公众假期来源**：香港公众假期已在 VSYS Leaves 日历里以 `HK-` 前缀全年列出，算天数时以此为准（也可参考 GovHK 官方清单）。
- 佐证：Employee 02「26–30 Jun = 3」（26 五 / 27-28 周末 / 29 一 / 30 二 = 3 个工作日）。
- ⚠️ **注意**：表格的"天数"≠日历事件跨度。日历整段多天事件含周末；表格只数工作日。两者分别处理。

---

## 4. 每条请假的登记流程（浏览器自动化）

触发：Phase 1/1b 的定时任务处理一封新请假/WFH 邮件、写完日历后 → 同步登记表格。

1. **定位 tab**：按请假人 → roster.json 的 preferredName → 找到对应 tab（仅限 8 人；不在 8 人内 → 跳过并报告）。
2. **算天数**：起止日期 → 工作日数（排除周末 + HK 公众假期），半天 0.5。
3. **查重**：读该 tab 明细，若同人同日期同类型已存在 → 跳过。
4. **追加明细行**：在明细区末尾写 From / To / Type / 天数 / Remarks（Remarks 记来源邮件主题）。
5. **更新 Summary**：对应类型列的 Total 累加；若为 AL，则 Balance = Entitled − AL Total（若用公式则自动）。
6. **校验**：Birthday Leave 的月份/天数限制；AL Balance 若为负 → 标红提示。
7. **回读确认**：写完后回读该行 + Summary，确认无误。

### 4.1 浏览器操作实操细则（Employee 01 试点 2026-07-16 验证）

每个 tab 的实际布局与操作要点：
- **明细列**：A=From, B=To, **C=Type（下拉验证格）**, D=No. of day(s) Taken, E=Remarks。明细从第 12 行起。
- **Summary**：第 7 行表头 `Summary|AL|SL|Comp|WFH|Others`(B–F)；第 8 行 = 各类已用；第 9 行 = Balance。
- **Summary 与 Balance 是公式自动算的**：`Balance(B9) = F3 − B8`（F3=Entitled AL，B8=AL 已用）；AL 已用是对明细的自动汇总。→ **只需填明细，Summary/Balance 自动更新，不要手动改**。
- **填写方式（避开下拉格 Tab 陷阱）**：**每一列都单独填**，用名称框跳到该列首格，逐行 `值⏎`。绝不在一个 type 动作里用 `\t` 跨列——**浏览器 type 里的 Tab 不会跳格**，会把多个值挤进同一格（2026-07-17 踩坑：From 和 To 都进了 A 列、B 列全空；数字总额靠 C/D 列算，全对，把人骗过去了）。
  - A 列（From）：名称框 → A首格 → `日期⏎日期⏎…`
  - B 列（To）：名称框 → B首格 → `日期⏎日期⏎…`（**单独一列，不跟 A 合并**）
  - C 列（Type）：名称框 → C首格 → `AL⏎AL⏎…`（下拉格接受"打字+回车"）
  - D 列（天数）/ E 列（Remarks）：各自名称框跳首格，逐行 `值⏎`
  - 公式格（如修 B6/F6）：**逐格单独输入 + 回车**，不要 `\t` 串联（Tab 会被公式自动补全吃掉，变成 FALSE）。
- **核对必须点开实际格子看公式栏**，不能信网格溢出显示（B 列空时，A 列会把两个值溢出显示成"好像两格都填了"）。回读 A/B 各一格确认。
- **删无依据条目**：日历上"无来源邮件"的可疑条目（例：Employee 01 2026-05-14 09:00-14:00，无 Source email、邮箱也查无）→ 与用户确认后，日历 + sheet 双删。

### 4.2 Balance 显示格式（用户 2026-07-16 定）

Balance 及天数**不取整**，必须显示小数（如 11.5，不能四舍五入成 12）。发现各 tab 格式不统一（Employee 01 曾设 0 位小数把 11.5 显示成 12；Employee 03 显示 13.0）。→ **检查所有 tab，凡 Balance/已用格设成整数的，统一改为显示 1 位小数**。

> **写入方式**：浏览器驱动 Google Sheets UI（连接器无写单元格能力）。约束：需 App 开着；界面变动可能出错；因此**在交互式会话里、你在场时执行并当场核对**，而非放进无人值守定时任务。

### 4.3 月度只读比对定时任务 `vsys-phase2-monthly-diff`（2026-07-21 定，已建）

- **时间**：每月**第一个星期一 10:00 HKT**。Cron 用 `0 10 * * 1`（每周一触发），任务 prompt 第一步自检"今天日期是否 ≤7"，不是则直接停止——用代码层面的自检替代 cron 字段本身对"第一个周一"语义的依赖（cron day-of-month 与 day-of-week 同时限定时是 OR 语义，容易踩坑）。
- **结构性只读**：只加载 Gmail/Calendar/Drive 的读工具，不加载任何写入或浏览器工具，杜绝无人值守时误写的可能。
- **流程**：读上月邮件确认的请假/WFH（同 §3 邮件为准规则）→ 日历 cross-check → 读表格现有明细去重 → 按 §3.4 算工作日天数 → 标记生日假月份/AL 余额异常 → 只输出"待登记清单"报告。
- **之后**：用户在场时说"同步表格"，才真正用浏览器写入（§4.1 方法）+ 回读核对。
- **跨月边界（2026-09-03 新增，必须做）**：窗口是按**邮件日期**划的，但请假日期可能落在下个月。这类条目**下个月不会再被搜到**（那封邮件不在下月窗口内）→ 必须在当次报告里**单列提醒**并记进 CHANGELOG 待办，不能静默丢弃。*实例：Employee 04 8/26 邮件申请 9/3 WFH + 9/4 AL。*
- **不要误报日历"类型不符"**：日历只有 `AL:/SL:/BL:` 前缀，Comp Leave 与 Birthday Leave 在日历上都归入 `AL:`（见 PRD §3）。日历写 `AL:` 而邮件说补假/生日假 → **正常，不是冲突**，表格按邮件细分即可。
- **顺带发现的表格格式/结构缺陷**（日期格式不一致、下拉验证没盖到新行、From/To 挤在一格）→ 在报告里列为"同步时顺手修"，由 §4.4 常设授权在同步阶段直接修掉。

---

### 4.4 表格维护动作：常设授权，直接做不用问（用户 2026-09-03 确认）

同步表格时遇到下列三类"格式/结构对齐"问题，**直接修掉**，修完在汇报里说明改了什么即可，不再逐次征询。三类都不改变任何请假事实（假别、天数、余额均不受影响）：

| 缺陷 | 修法 | 首次实例（2026-09-03） |
|---|---|---|
| 日期显示格式不一致 | 用格式刷从该 tab 的 From 列刷到 To 列，统一成 `d-Mmm-yyyy` | Employee 06 tab B10:B20 原显示 `30-6-2026`/`7-8-2026` → 统一（底层值本来就对） |
| 下拉验证范围没盖住新行 | 复制一个带验证的 Type 格 →「选择性粘贴 → 仅数据验证」向下延伸 | Nancy tab C22 "Birthday Leave" 在验证范围外 → 验证延到 C40 |
| 历史 From/To 挤在同一格 | 拆开填回 A/B 两列；**改之前先用 D 列天数 + Remarks 交叉验证**正确日期，不猜 | Employee 04 tab 第 12 行 A 格内含 `16-Apr-2026⇥22-Apr-2026`、B 错成 `4-May-2026` → 改为 A=16-Apr、B=22-Apr（D=5 天与 4/16–4/22 的 5 个工作日吻合） |

⚠️ **授权边界**：只覆盖上述"表面对齐"。凡会**改变假别 / 天数 / 余额**，或**删除既有明细行**的操作，仍须先问用户（沿用 §4.1 末尾"删无依据条目"的流程）。

---

## 5. 数据源权威性：邮件 = 最终准，日历 = cross-check（2026-07-16 确立）

- **录入表格一律以邮件为最终确认版本**；日历只用来交叉核对，冲突时**以邮件为准**。
- 原因：日历是从邮件派生的，且被旧自动化污染过（见下）。天数口径也不同（日历含周末整段；表格只数工作日）。
- **每条录入前**：必须找到"此人本人"发出的原始请假邮件；只在别人串里以审批/回复出现的 → 不录（审批人≠请假人）。

### 5.1 已知污染 + 全日历清扫（2026-07-16）
旧自动化（6/30 版）系统性 bug：把**回复/审批邮件的人**当成请假人，给经理/审批人凭空造假条目。已确认误记例：Employee 03 5/11（来自 Employee 36 辞职邮件的"最后一天"）、Employee 04 5/11 & 6/23 WFH（批 Employee 40/Employee 02）、Employee 04 4/10 AL（批 Employee 41 WFH）、Employee 01 5/14（无据）、Employee 10 ×3、Employee 09 6/29。
→ 执行一次**两日历全量清扫**：逐条对邮件验证，凡"审批误记 / 无邮件依据"的删除；不确定的留给人工。

---

## 6. 待办 / 待确认（实施前）

1. **每人生日月份**：Birthday Leave 校验需要。roster/表格里暂无——需你提供 8 人的生日（至少月份）。
2. **Comp Leave（补假）来源规则**：怎么产生、怎么扣减？目前只见结余列，未见发放规则。（Employee 02/Employee 03 有 Comp Leave 相关行）
3. **历史数据对齐**：现有 tab 里已有 2026 记录，且与日历偶有出入。要不要先做一次"对齐核对"，还是只管往后新增？
4. **公众假期表**：算工作日需要香港公众假期清单（日历里已有 HK- 系列，可复用）。

---

## 7. 决策记录（本设计依据）

- Birthday Leave → Summary 单独一列（用户 2026-07-15 定）。
- 年底结转 → Option A（公式）+ C（人工复核），无买断（用户确认公司只有 carry-over）。
- 表格写入 → 浏览器自动化（连接器目录无可写 Sheets 连接器；用户选路 2）。
- 范围 → 先现有 8 个 tab。
- 只有 AL 扣年假；其余假别单独计不扣 AL；生日假限当月 1 天。
